/*
 * This is sample code generated by rpcgen.
 * These are only templates and you can use them
 * as a guideline for developing your own functions.
*/

#include "array.h"


void
add_prog_1(char *host,int row_first,int col_first,int row_sec,int col_sec)
{
	CLIENT *clnt;
	int  *result_1;
	arrays  add_1_arg;

#ifndef	DEBUG
	clnt = clnt_create (host, ADD_PROG, ADD_VERS, "udp");
	if (clnt == NULL) {
		clnt_pcreateerror (host);
		exit (1);
	}
#endif	/* DEBUG */

	// input the first matrix
	int first_arr[row_first*col_first];
	printf("Enter the data for the first array\n",);
	for(int i = 0;i < row_first*col_first;i++) scanf("%d",&first_arr[i]);

	add_1_arg.row_first = row_first;
	add_1_arg.col_first = col_first;
	add_1_arg.first = first_arr;

	// input the second matrix
	int second_arr[row_sec*col_sec];
	printf("Enter the data for the second array\n",);
	for(int i = 0;i < row_sec*col_sec;i++) scanf("%d",&second_arr[i]);

	add_1_arg.row_sec = row_sec;
	add_1_arg.col_sec = col_sec;
	add_1_arg.second = second_arr;

	result_1 = add_1(&add_1_arg, clnt);
	if (result_1 == (int *) NULL) {
		clnt_perror (clnt, "call failed");
	}else{
    printf("Result of matrix multiplication is :%d\n",*result_1);
  }

#ifndef	DEBUG
	clnt_destroy (clnt);
#endif	 /* DEBUG */
}


int
main (int argc, char *argv[])
{
	char *host;

	if(argc < 6){
		printf ("usage: %s server_host\n", argv[0]);
		exit (1);
	}

	host = argv[1];
	add_prog_1 (host,atoi(argv[2]),atoi(argv[3]),atoi(argv[4]),atoi(argv[5]));

exit (0);
}
