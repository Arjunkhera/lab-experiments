/*
 * This is sample code generated by rpcgen.
 * These are only templates and you can use them
 * as a guideline for developing your own functions.
 */

#include "array.h"

int *
add_1_svc(arrays *argp, struct svc_req *rqstp)
{
	static int  result;

	int first_matrix[argp->row_first][argp->col_first];
	for(int i = 0;i < row_first;i++)
		for(int j = 0;j < col_first;j++)
			first_matrix[i][j] = argp->first[i*col_first+j];

	int second_matrix[argp->row_sec][argp->col_sec];
	for(int i = 0;i < row_sec;i++)
		for(int j = 0;j < col_sec;j++)
			second_matrix[i][j] = argp->second[i*col_first+j];

	int answer_matrix[row_first][col_sec];
	for(int i = 0;i < row_first;i++)
		for(int j = 0;j < col_sec;j++){
			answer_matrix[i][j] = 0;
			for(int k = 0;k < col_first;k++)
				answer_matrix[i][j] += first[i][k]*second[k][j];
		}

	return &result;
}
