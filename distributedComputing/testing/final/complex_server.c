/*
 * This is sample code generated by rpcgen.
 * These are only templates and you can use them
 * as a guideline for developing your own functions.
 */

#include "complex.h"

int *
arith_1_svc(complex *argp, struct svc_req *rqstp)
{
	static int result;

	/*
	 * insert server code here
	 */

	int real = 0;
	int imag = 0;
	if(argp->option == 1){
		real = argp->first_real+argp->second_real;
		imag = argp->first_imag+argp->second_imag;
	}
	else if(argp->option == 2){
		real = argp->first_real-argp->second_real;
		imag = argp->first_imag-argp->second_imag;
	}

	printf("The imaginary part is: %d",imag);

	result = real;
	return &result;
}
